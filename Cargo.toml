[workspace]
resolver = "2"
members = ["crates/csm-rs"]
# Parked (grilling Q12): crates/csm-horus-node joins once the library validates.

[workspace.package]
edition = "2021"
# The current crate includes the GPL-2.0-or-later gpc-derived solver.
# Revisit this metadata only after a clean-room solver replaces it.
license = "GPL-2.0-or-later"
repository = "https://github.com/LotfiZ/csm-rs"

[profile.release]
lto = true
